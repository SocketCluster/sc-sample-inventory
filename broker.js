// https://www.npmjs.com/package/sc-rabbitmq
// npm install sc-rabbitmq

// var scRabbit = require('sc-rabbitmq');

module.exports.run = function (broker) {
    console.log('   >> Broker PID:', process.pid);

    // scRabbit.attach(broker);
};
